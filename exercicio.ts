function multiplicaNumero(num1: number, num2: number):number {
    return num1 * num2;
}

function nomeMaisTexto(nome: string): string {
    const saudacao = "Olá " + nome; 
    return saudacao;
}